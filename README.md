Following scripts should be run with sudo access.

## Steps to follow

``` 
git clone https://github.com/promehul/Some-useful-scripts.git
```

``` 
sudo ./{script_name} 
``` 
to run any script after cloning the repository